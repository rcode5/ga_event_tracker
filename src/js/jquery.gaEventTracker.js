// Generated by CoffeeScript 1.4.0
(function() {

  $.fn.gaEventTracker = function(gaq, category, action, label) {
    var getAction, getCategory, getLabel;
    getCategory = function() {
      return $(this).data('category');
    };
    getAction = function() {
      return $(this).data('action');
    };
    getLabel = function() {
      return $(this).data('label');
    };
    return this.each(function() {
      return $(this).bind("click", function(ev) {
        var args, target;
        target = this;
        category || (category = getCategory);
        action || (action = getAction);
        label || (label = getLabel);
        args = $.map([category, action, label], function(entry) {
          var val;
          console.log(entry);
          if (typeof entry === "function") {
            try {
              val = entry.apply(target);
            } catch (_error) {}
            return val || 'empty';
          } else {
            return entry;
          }
        });
        gaq.push(["_trackEvent", args[0], args[1], args[2]]);
        return true;
      });
    });
  };

}).call(this);
